1. To create a work pool:
   prefect work-pool create 'my-work-pool'

2. To start a work pool:
   prefect work-pool start 'my-work-pool'

3. To stop a work pool:
   prefect work-pool stop 'my-work-pool'
